  
  <h2>{{name}}</h2>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    {{#each comments as |comment|}}
  
   <p> {{comment.comment}} Made by {{../user}} {{user.name}} on {{format_date date_created}} </p>
   
    {{/each}}
  </p>
  <textarea id="commentInput">

  </textarea>
  <p>
    <button data-id={{id}} id='commentSubmit' class="btn btn-primary">Comment</button>
  </p>
</div>
<script src="/js/comment.js"></script>